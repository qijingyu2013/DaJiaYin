 @extends('base.base')
 @section('content')
 <div class="container">
    <div class="content">
        <div class="title">{{ $name }}</div>
    </div>
</div>
 @stop

 @for ($ = 0; i < ; $i++)
 	{{-- expr --}}
 @endfor

@foreach ($array as $element)
	{{-- expr --}}
@endforeach